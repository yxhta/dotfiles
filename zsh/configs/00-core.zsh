# ==========================================================================
# Core Shell Options
# ==========================================================================

# Prevent accidental logout with Ctrl+D
setopt ignoreeof

setopt no_beep
setopt auto_cd
setopt multios
setopt correct
setopt interactive_comments
